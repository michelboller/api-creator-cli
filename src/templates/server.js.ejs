const express = require('express')
const fs = require('fs')
const app = express()
const port = process.env.PORT || 3000


fs.readdirSync("./api/src/router/").forEach( file => require("./router/"+file)(app))

app.listen(port, () => {
    console.log(`Listening at port: ${port}`)
})